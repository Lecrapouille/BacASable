# Mock C++ Code

- **Mock1**: Tests mocking C functions such as read, open, ioctl. These functions are used for communication protocols such as UART, I2C, SPI.
- **Mock2**: Intercepts arguments in a non-virtual class nested inside a PIMPL implementation (so private).
