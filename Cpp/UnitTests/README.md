# Mock Cpp code

- Mock1 tests mocking C functions such read, open, ioctl. These functions are using for communication protocol such as UART, I2C SPI.
- Mock2 intercept arguments in a non virtual class nested inside a PIMPL implementation (so private).
